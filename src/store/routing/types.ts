const prefix: 'ROUTING' = 'ROUTING';
const SET_LOCATION = `${prefix}.SET_LOCATION`;
const SET_AUTH_CONFIG = `${prefix}.SET_AUTH_CONFIG`;
const LINK_TO = `${prefix}.LINK_TO`;

export default {
  prefix,
  SET_LOCATION,
  SET_AUTH_CONFIG,
  LINK_TO,
};
